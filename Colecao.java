import  java.util.HashList;
public class Colecao {
    public int[] twoSum(int[] nums, int target) {
        Hash<Integer, Integer> lista = new HashList<>();
        for (int i = 0; i < nums.length; i++) {
            int complemento = target - nums[i];
            if(lista.contains)
        }
    }
}